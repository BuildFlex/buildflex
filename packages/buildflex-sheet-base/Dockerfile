# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build buildflex-sheet-base`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t buildflex-sheet-base`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system buildflex-sheet-base && \
          adduser --system -G buildflex-sheet-base buildflex-sheet-base

COPY  buildflex-sheet-base
RUN chown -R buildflex-sheet-base:buildflex-sheet-base .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix buildflex-sheet-base --omit=dev -f install

CMD [ "node", "buildflex-sheet-base" ]
