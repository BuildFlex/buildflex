# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build buildflex-server`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t buildflex-server`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system buildflex-server && \
          adduser --system -G buildflex-server buildflex-server

COPY  buildflex-server
RUN chown -R buildflex-server:buildflex-server .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix buildflex-server --omit=dev -f install

CMD [ "node", "buildflex-server" ]
