# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build buildflex-sheet-base-e2e`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t buildflex-sheet-base-e2e`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system buildflex-sheet-base-e2e && \
          adduser --system -G buildflex-sheet-base-e2e buildflex-sheet-base-e2e

COPY  buildflex-sheet-base-e2e
RUN chown -R buildflex-sheet-base-e2e:buildflex-sheet-base-e2e .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix buildflex-sheet-base-e2e --omit=dev -f install

CMD [ "node", "buildflex-sheet-base-e2e" ]
