# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build buildflex-builder`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t buildflex-builder`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system buildflex-builder && \
          adduser --system -G buildflex-builder buildflex-builder

COPY  buildflex-builder
RUN chown -R buildflex-builder:buildflex-builder .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix buildflex-builder --omit=dev -f install

CMD [ "node", "buildflex-builder" ]
