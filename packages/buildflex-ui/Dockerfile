# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build buildflex-ui`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t buildflex-ui`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system buildflex-ui && \
          adduser --system -G buildflex-ui buildflex-ui

COPY  buildflex-ui
RUN chown -R buildflex-ui:buildflex-ui .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix buildflex-ui --omit=dev -f install

CMD [ "node", "buildflex-ui" ]
